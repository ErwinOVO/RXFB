# 法规库检索服务设计（路线 C）

> 版本 v1 · 2026-09-11 · 状态：设计稿（未实施）
> 目标读者：本项目开发者 / 后续实施者
> 相关文件：`法规库.db`、`法规库数据库设计.sql`、`入库/查看法规库.py`

---

## 0. 一句话目标

把 2897 条法规库包成一个**检索服务**，同时服务三种消费方——Web 检索页、Dify AI 检索、命令行脚本；
检索内核 = **结构化检索（SQL）＋ 语义检索（向量）** 的混合，输出统一结构。

**核心原则：库的所有权留在本地。** Dify 只做消费方，不托管数据、不切分、不做索引。
这样法询式的「时效 / 机构 / 分类」元数据过滤能力，在 AI 检索场景里才不会丢失。

---

## 1. 边界

**做：**
- 查询、检索、排序、返回结果与全文
- 暴露筛选维度（层级 / 机构 / 分类 / 时效 / 年份）
- 对 Dify 提供符合其规范的外部知识库检索接口

**不做：**
- 不写库（数据变更仍走 `入库/` 下的脚本）
- 不做 Dify 侧的知识库管理（Dify 对外部库只有检索权限，这是平台限制）
- 不碰 `penalty_case` / `internal_policy`（二期）

---

## 2. 架构

四层结构（见对话内架构图）：

| 层 | 组件 | 说明 |
|---|---|---|
| 消费层 | Web 检索页 / Dify 应用 / 命令行 | 三种入口，共用同一服务 |
| 接口层 | `/api/*` + `/retrieval` | 前者自有，后者遵守 Dify 规范 |
| 引擎层 | SQL 引擎 / 向量引擎 / 混合重排 | SQL 管精确与过滤，向量管语义 |
| 数据层 | `法规库.db` / 向量索引 / `reg_chunk` 表 | 切片是新增的数据产物 |

---

## 3. 三个关键设计决策

### 3.1 切片按「条款」，不按「字数」

法询的检索颗粒度是到条款的。法规库应当照此处理，理由：

1. **法规天然以「条」为语义单元** —— 「第十二条」是一个完整规范，跨条切开会切断语义。
2. **引用以条为单位** —— 未来做 `reg_relation` 条款级关联时，chunk 与条款一一对应，直接可挂。
3. **颗粒度适中** —— 太长（整篇）会稀释语义向量，太短（句）会丢上下文。

**切片规则：**

| 情形 | 处理 |
|---|---|
| 有「第 X 条」结构 | 每条一片，`chunk_type = article` |
| 单条 > 800 字 | 在条款内按句号再切，保留条号前缀 |
| 无条款结构（通知 / 公告 / 批复） | 500 字滑窗，重叠 100 字，`chunk_type = window` |
| 章标题、前言 | 单独成片，`chunk_type = chapter / preamble` |

**每片必须拼上下文前缀**，否则向量召回不准：

```
《个人住房贷款管理办法（试行）》（金规〔2026〕X号，国家金融监督管理总局）第十二条  商业银行发放个人住房贷款，应当……
```

标题 + 文号 + 机构 + 条号作为前缀一起嵌入，用户用自然语言问「房贷首付比例怎么算」时才能命中。

### 3.2 不让 Dify 托管向量索引

| 维度 | 模式 A：外部知识库（**推荐**） | 模式 B：灌入 Dify 内置库 |
|---|---|---|
| 数据位置 | 留在本地 | 复制进 Dify |
| 元数据过滤 | **保留**（时效 / 机构 / 分类） | 丢失，只剩相似度 |
| 切片控制 | 我们按条款切 | Dify 自动按字数切 |
| 更新方式 | 改本地库 + 重建索引 | 重新上传、重跑 |
| 运维成本 | 需自建向量索引 | 省事 |

**选 A。** 理由：法询相对通用 RAG 工具的竞争力，恰恰是「结构化元数据 × 全文」的联合检索。
模式 B 会把 `status`（有效 / 已废止）、`issuer`、`category` 这些维度全丢掉，
用户问「现行有效的房贷规定」时无法排除已废止文件——这对合规场景是致命的。

### 3.3 混合检索——不能只做向量

向量擅长「意思相近」，但有三个盲区：

| 盲区 | 例子 | 向量会怎样 |
|---|---|---|
| 精确文号 | `银发〔2025〕205号` | 可能召回到文号相近的其他文件 |
| 精确法条引用 | `《反洗钱法》第十二条` | 无法保证精确定位 |
| 时效过滤 | 「现行有效」 | 完全不理解这个概念 |

所以必须 **SQL（元数据过滤 + FTS5 关键词）＋ 向量（语义召回）→ RRF 融合**。
RRF（Reciprocal Rank Fusion）不需要两路分数可比，只按排名融合，实现简单、效果稳。

---

## 4. 接口设计

### 4.1 Dify 外部知识库接口（核心）

这是整份设计的重点。Dify 的调用契约（已按其官方文档核实）：

**请求**
```
POST {base_url}/retrieval
Content-Type: application/json
Authorization: Bearer {API_KEY}
```
> Dify 会在你配置的 API Endpoint 后**自动追加 `/retrieval`**，所以你注册时只填基地址。

```json
{
  "knowledge_id": "jinku-regulation",
  "query": "商业银行房地产贷款集中度管理要求",
  "retrieval_setting": {
    "top_k": 5,
    "score_threshold": 0.5
  },
  "metadata_condition": {
    "logical_operator": "and",
    "conditions": [
      { "name": "status", "comparison_operator": "is", "value": "有效" },
      { "name": "issuer", "comparison_operator": "contains", "value": "中国人民银行" }
    ]
  }
}
```

**响应（HTTP 200）**
```json
{
  "records": [
    {
      "content": "《个人住房贷款管理办法（试行）》（金规〔2026〕X号）第十二条 商业银行发放个人住房贷款，应当……",
      "score": 0.87,
      "title": "国家金融监督管理总局关于印发《个人住房贷款管理办法（试行）》的通知",
      "metadata": {
        "reg_id": 698,
        "doc_number": "金规〔2026〕X号",
        "issuer": "国家金融监督管理总局",
        "hierarchy": "规范性文件及通知",
        "status": "有效",
        "pub_date": "2026-08-28",
        "effective_date": "2026-10-01",
        "category": "房地产",
        "article_no": "第十二条",
        "source_url": "https://www.nfra.gov.cn/cn/view/pages/ItemDetail.html?docId=..."
      }
    }
  ]
}
```

**必须遵守的硬性约束（否则 Dify 管线报错）：**

1. `metadata` 必须是对象 `{}`，**不能是 `null`** —— 这是 Dify 官方明确点出的最常见的踩坑点。
2. 每条记录必须有 `content` 和 `score`。
3. 非 200 状态码会直接抛错给终端用户；空结果要返回 `{"records": []}` 而不是 404。
4. `score` 范围 0~1。

**我们的实现要点：**

- **`knowledge_id` 当路由用** —— 把不同的「检索视图」编码进 knowledge_id，一套服务对外呈现多个知识库：

| knowledge_id | 含义 | 默认过滤 |
|---|---|---|
| `jinku-all` | 全库 | 无 |
| `jinku-current` | 现行有效 | `status = 有效` |
| `jinku-pbc` | 仅人民银行 | `issuer` 含人民银行 |
| `jinku-nfra` | 仅金监总局 | `issuer` 含金监总局 |

- **`metadata_condition` → SQL WHERE 翻译器** —— 这是把法询的筛选能力暴露给 Dify 的唯一入口。Dify 目前没有 UI 配置它（仅供程序化调用），但**未来 Dify 在 Agent / 工作流里自动生成过滤条件时，我们的服务已经能接**。支持的运算符需全量覆盖：`contains` / `not contains` / `start with` / `end with` / `is` / `is not` / `in` / `not in` / `empty` / `not empty` / `=` / `≠` / `>` / `<` / `≥` / `≤` / `before` / `after`。

- **`retrieval_setting.score_threshold`** —— 注意 Dify 在用户禁用阈值时传 `0.0`，此时不要做任何过滤。

- **分数归一化** —— 混合检索的原始分（BM25 分 + 余弦相似度）量纲不同，必须映射到 0~1 再返回，否则 Dify 的阈值过滤行为不可预期。

### 4.2 自有结构化检索接口（给 Web 用）

| 方法 | 路径 | 用途 |
|---|---|---|
| POST | `/api/search` | 结构化检索（关键词 + 多维筛选 + 分页） |
| POST | `/api/semantic` | 纯语义检索（调试 / 高级用户） |
| GET | `/api/regulation/{id}` | 取单条全文（正文按需加载） |
| GET | `/api/filters` | 拉筛选树（层级 / 机构 / 分类 / 时效 + 各节点计数） |
| GET | `/api/stats` | 库概况（总数、层级分布、时效分布） |
| GET | `/api/export` | 当前结果集导出 CSV |

`POST /api/search` 请求体：
```json
{
  "q": "贷款",
  "org": ["中国人民银行"],
  "category": ["房地产"],
  "hierarchy": ["部门规章", "规范性文件及通知"],
  "status": ["有效"],
  "date_from": "2024-01-01",
  "date_to": null,
  "page": 1,
  "size": 20,
  "sort": "relevance"
}
```

响应统一结构（供前端直接渲染）：
```json
{
  "total": 63,
  "page": 1,
  "hits": [
    {
      "reg_id": 698,
      "title": "……",
      "doc_number": "金规〔2026〕X号",
      "issuer": "国家金融监督管理总局",
      "hierarchy": "规范性文件及通知",
      "status": "有效",
      "pub_date": "2026-08-28",
      "category": ["房地产"],
      "snippet": "……命中位置前后 120 字……",
      "score": 0.87,
      "matched_by": "hybrid"
    }
  ],
  "facets": {
    "hierarchy": { "部门规章": 12, "规范性文件及通知": 51 },
    "issuer": { "国家金融监督管理总局": 38, "中国人民银行": 25 },
    "status": { "有效": 55, "已废止": 8 }
  }
}
```
> `facets` 返回**当前结果集**的聚合计数，前端筛选树的数字由此驱动（法询的筛选树就是这种「动态计数」体验）。

### 4.3 智能路由——一个搜索框

Web 前端只暴露一个输入框，由后端判断走哪条路，前端不感知：

| 输入特征 | 路由 | 理由 |
|---|---|---|
| 含文号模式（`〔〕`/`[]`/`号`） | SQL 精确 | 文号是强标识，向量反而会串 |
| 长度 1~2 字 | SQL LIKE | FTS5 trigram 需 ≥3 字，短词覆盖不到 |
| 3~15 字 | FTS5 为主 + 向量补充 | 关键词明确时精确匹配更可靠 |
| > 15 字 / 含疑问词（吗 / 如何 / 怎么 / 什么 / 为什么） | 向量为主 + FTS 补充 | 自然语言提问，语义匹配更合适 |
| 始终 | 三者结果都过 RRF | 兜底，避免漏召 |

---

## 5. 数据层扩展

### 5.1 新增 `reg_chunk` 表

```sql
CREATE TABLE reg_chunk (
  id            INTEGER PRIMARY KEY,
  reg_id        INTEGER NOT NULL REFERENCES regulation(id),
  chunk_index   INTEGER NOT NULL,   -- 篇内顺序
  chunk_type    TEXT,               -- article / chapter / window / preamble
  article_no    TEXT,               -- '第十二条'
  heading       TEXT,               -- 所属章节标题
  text          TEXT NOT NULL,      -- 切片正文（含上下文前缀）
  char_len      INTEGER,
  token_est     INTEGER,
  embedding_id  TEXT,               -- 向量索引里的主键
  embed_model   TEXT,               -- 记录用的是哪个 embedding 模型
  content_hash  TEXT,               -- 增量重建用：hash 不变则跳过重嵌
  created_at    TEXT
);
CREATE INDEX idx_chunk_reg  ON reg_chunk(reg_id);
CREATE INDEX idx_chunk_hash ON reg_chunk(content_hash);
```

`content_hash` 是**增量重建的关键**：切片规则或原文变动时才重嵌，否则全量重跑一次 3~5 万 chunk 的 embedding 成本很高。

### 5.2 向量库选型

预估切片量：2897 条 × 平均 12~15 条/篇 ≈ **3.5~4.5 万 chunk**，规模很小。

| 方案 | 优点 | 缺点 | 建议 |
|---|---|---|---|
| **sqlite-vec** | 和现有 `.db` 同库、零额外服务、纯 Python 装 | 功能较少，规模大了性能弱 | **推荐起步**（与当前架构最贴） |
| Qdrant | 过滤能力强、Docker 一条命令、可水平扩 | 多一个服务要维护 | 数据量大或要生产化时 |
| pgvector | 若有 PostgreSQL 生态 | 要引入 PG | 已有 PG 时 |
| FAISS | 纯库、极快 | 无服务化、持久化要自己写 | 只做离线实验 |

**起步建议 sqlite-vec**：零运维、和 `法规库.db` 一起备份、单机场景完全够用。

### 5.3 Embedding 模型

| 选择 | 说明 |
|---|---|
| **BGE-M3**（推荐） | 中文表现好、支持长文本、中英混合、本地免费可离线 |
| bge-large-zh-v1.5 | 纯中文更轻，但长文本弱 |
| 通义 text-embedding-v3 / 其他 API | 免本地算力，但要联网、按量计费 |

法规文本单条较长，**BGE-M3** 更合适。`embed_model` 字段要记录模型名，未来换模型能识别出哪些 chunk 需重嵌。

---

## 6. 前端设计（Web 检索页）

单文件 HTML + 原生 JS，由服务托管；不引构建工具、不引外部 CDN（你一贯的偏好）。

**布局仿法询：**

```
┌─────────────────────────────────────────────────────────┐
│  [搜索框.........................]  [智能/精确]  [导出]  │
├──────────────┬──────────────────────────────────────────┤
│ 筛选树        │  结果列表                                 │
│  层级  (14)   │  ┌─────────────────────────────────────┐ │
│  机构  (36)   │  │ 《XX办法》          [有效] 2026-08-28 │ │
│  分类  (57)   │  │  金规〔2026〕X号 · 金监总局 · 房地产   │ │
│  时效          │  │  ……命中摘要，关键词高亮……            │ │
│   有效  (1240) │  └─────────────────────────────────────┘ │
│   已废止 (242) │  ┌─────────────────────────────────────┐ │
│  年份         │  │ ……                                   │ │
├──────────────┴──────────────────────────────────────────┤
│  详情弹层：条款定位 + 元数据 + 原文链接 + 关联法规        │
└─────────────────────────────────────────────────────────┘
```

**要点：**
- 左侧筛选树每个节点带**动态计数**（来自 `facets`），选了就收窄
- 结果列表只回摘要（`snippet`），**正文按需加载** —— 2897 条正文全内联网页会到 50MB+
- 详情弹层里按「条款」呈现（因为 chunk 就是按条切的），可定位到具体条
- 时效徽章用色区分：有效 / 已废止 / 已修订 / 失效

---

## 7. 技术选型汇总

| 组件 | 选型 | 理由 |
|---|---|---|
| 服务框架 | FastAPI + uvicorn | 你项目已用 FastAPI，栈统一 |
| 事实源 | SQLite `法规库.db` | 已有 |
| 全文检索 | FTS5（已有 `reg_fts`） | 已有 |
| 向量索引 | sqlite-vec（起步）/ Qdrant（生产） | 见 5.2 |
| Embedding | BGE-M3 | 见 5.3 |
| 前端 | 单文件 HTML + 原生 JS | 你的一贯偏好，零依赖 |
| Dify 集成 | 外部知识库 API（模式 A） | 见 3.2 |

---

## 8. 实施阶段

| 阶段 | 内容 | 依赖 | 产出 |
|---|---|---|---|
| S0 | 补 `status`（正文含废止证据的可正则捞） | — | 时效覆盖 ↑ |
| S1 | 切片脚本 → `reg_chunk` 表 | S0 | 3~4 万 chunk |
| S2 | 向量化 → 向量索引 | S1 | embedding 完成 |
| S3 | FastAPI 服务：`/api/*` | S1 S2 | 结构化 + 语义可用 |
| S4 | `/retrieval` + 与 Dify 联调 | S3 | Dify 能检索 |
| S5 | Web 检索页 | S3 | 可视化入口 |
| S6 | 混合重排调优、facets 完善 | S3–S5 | 体验打磨 |

> S0 放在最前，因为 `status` 现在只有 51% 覆盖，**时效过滤会大面积失效**——而时效是合规检索里最重要的筛选维度。先补它，后面 S1 切片的元数据才完整。

---

## 9. 风险与注意事项

1. **Dify 自托管有 SSRF 代理** —— 出站 HTTP 走 Squid，需要把本服务域名加进 `docker/ssrf_proxy/squid.conf.template` 的 `allowed_domains`，否则连接被拒。若 Dify 在另一台机器，还要保证网络可达。
2. **`status` 覆盖 51%** —— 见 S0，必须先补，否则 `jinku-current` 视图和时效筛选都不可信。
3. **`scope_sub` 字段 0% 覆盖** —— 暂不暴露为筛选维度。
4. **未分类 564 条（19%）** —— 分类筛选会漏，建议后续补词重跑 `入库/重跑分类.py`。
5. **修订版本混淆** —— 同一文号可能有多个版本（如某《办法》2021 版与 2025 修订版），检索时要靠 `status` + `effective_date` 排除旧版，避免答出过期条款。
6. **embedding 重建成本** —— 切片规则一改就要全量重嵌，`content_hash` 增量机制必须设计正确。
7. **条款切分的容错** —— 部分老文件的「第 X 条」格式不规范（全角空格、换行错位），切片器需要容错，切不出条款时降级为滑窗。
8. **端口与鉴权** —— 本机服务默认只监听 127.0.0.1；给 Dify 用时才开外网，且必须校验 Bearer Key（Dify 只传不校验，校验逻辑在我们这边）。

---

## 10. 与法询的能力对齐

| 法询能力 | 本设计对应 | 状态 |
|---|---|---|
| 法规全文检索 | FTS5 + `/api/search` | 数据已具备，待包服务 |
| 多维筛选树 | `facets` + 筛选树 UI | 待实施（S3/S5） |
| AI 语义检索 | 向量引擎 + `/retrieval` → Dify | 本设计新增 |
| 条款级定位 | `reg_chunk` 按条切片 | 本设计新增 |
| 条款级引用关联 | `reg_relation`（字段已预留） | 二期 |
| 处罚案例库 | `penalty_case`（空表） | 二期，需数据源 |
| 制度库比对 | `internal_policy`（空表） | 二期，数据在内网 |
| 时效动态 | `status` 补全 + 时效筛选 | S0 |

---

_本设计为「先设计、后实施」产物，实施前请确认 S0 与向量库选型两项决策。_
