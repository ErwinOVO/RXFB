# 法规数据库：分类标签 · 入库流程 · 新规维护方案

> 用途：银行合规知识库"地基"阶段的设计文档
> 状态：初稿，待评审
> 配套文件：《法规库数据库设计.sql》

---

## 一、法规怎么分类、怎么打标签

### 1.1 核心原则（先立规矩，避免后期返工）

1. **能放进字段的，不放进分类**——层级、效力、机关、日期已有独立字段，别再做成标签
2. **能自动推导的，不手工打**——分类尽可能用关键词规则自动映射，人工只核验存疑的
3. **分类是树、标签是点**——分类固定层级用于筛选和统计；标签灵活，多维打点用于召回
4. **宁少勿多**——每个分类/标签都要有明确的检索用途，开一堆没人用的标签是负债

### 1.2 分类体系（树状，多对多：一篇法规可挂多个分类）

**定稿：全量照搬法询智库分类结构（11 个分组 + 40 个专题），不限定银行三组。**

**决策背景（2026-09-09）**：全量建好分类，当前数据只涉及银行条线就先命中银行相关分类；将来扩展证券/保险/财税等客户条线时分类直接可用，无需改表。空分类不影响任何功能。

**结构（完整种子数据已写入《法规库数据库设计.sql》，id 段位：分组 1-11，专题 101+）：**

| 分组 | 专题 |
|------|------|
| 大资管 | 信托 / 理财 / 保险资管 / 私募基金 / 大资管综合 |
| 内控合规 | 反洗钱 / 内审规定 / 消费者保护 / 案件防控 / 数据与个保法 |
| 金融市场 | 票据法规 / 资金业务 / 债券市场 / 地方金融 |
| 银行业务 | 财富管理 / 不良资产 / 专精特新 / 房地产 / 小微贷款和普惠金融 / 互联网贷款 |
| 证券期货 | 全面注册制 / 证券基金监管 / 期货 / 投行业务 / REITS |
| 国际业务 | 外汇跨境 / 跨境人民币 / QFLP政策 / 利用外资 / ODI / QDLP |
| 政府投融资 | 土地制度 / 地方债 / 国企合规 / 绿色融资标准 / 政府采购 / 城市更新 |
| 财税会计 | 税务合规 / 会计准则 / 外贸外资税收 |
| 科创产业 | 人工智能 / 民营小微 |
| 保险 | 保险业常用法规 |
| 其他综合 | 海南自贸政策 / 民法典 / 金融标准 / ESG / 劳动人事 / 贸易融资与供应链金融 |

**归类的三个细则：**
- **主分类 1 个**（用于列表统计、卡片展示），**辅助分类不限**（用于筛选召回）——表中用 `reg_category` 多对多即可，另留 `is_primary` 标记
- 跨条线法规按"主要约束对象"归主分类，相关条线挂辅助分类
- **分类命中词库按数据实际覆盖面逐步扩**：当前只维护银行相关专题（反洗钱、消保、信贷等）的关键词映射表，证券/保险等专题等有数据了再补规则，不提前空转

### 1.3 标签体系（点状，最小集起步）

**推荐只开 3 类、每类不超过 6 个，验证有真实检索需求后再扩：**

| 标签组 | 标签候选 | 用途 |
|--------|---------|------|
| 性质标签 | 重要/重大、试行、暂行 | 首页"重点提示"类场景 |
| 监管热点 | 关联近期罚单密集案由的词（如"贷款三查""代客操作"） | 处罚关联场景召回 |
| 处置状态 | 待核验、人工待补元数据 | 内部工作流，不进前台 |

> 反例（不要这么干）：把"银保监发""人民银行令"做成标签——那是 `issuer` 字段；把"有效/失效"做成标签——那是 `status` 字段。

### 1.4 入库时必填 vs 选填（数据质量红线）

**必填（缺了就不算合格记录，直接进"待补"清单）：** 标题、发布机关、发布日期、效力状态、主分类、正文全文
**选填但强烈建议：** 文号、生效日期、辅助分类、来源链接
**可选：** 全文路径、备注

---

## 二、清洗入库流程——怎么做到"可复用"

### 2.1 可复用的关键：做成五段式管道，而不是一次性脚本

```
[1 接入] → [2 清洗标准化] → [3 元数据补全/打标] → [4 质量校验] → [5 入库+关联]
  输入原始爬虫数据           去乱码·去重·统一格式     规则提取字段+自动分类      不合格打回[2]      事务写入8张表
```

每段之间用**中间表/中间文件**衔接，好处：
- **断点续跑**：第 4 步发现 100 条不合格，只需重跑这一段，不用从头再来
- **换源不换管道**：将来客户行制度文件入库、处罚案例换数据源，只需新写"[1 接入]"的适配器，后面四段原样复用
- **可审计**：每段留日志，能回答"这批数据是谁、什么时候、怎么进来的"

### 2.2 各段要点

**① 接入**：读入原始数据（先支持：爬虫 JSON / CSV / Excel / 网页 HTML 四类，PDF 后续再补）
**② 清洗标准化**：
- 正文去 HTML 标签、去页眉页脚、统一全角半角与空白
- 去重规则：`标题归一化 + 文号` 双键判重（法规重名少见，文号缺失时用 标题+发布日期）
- 标题去书名号、去冗余空格
**③ 元数据补全 / 自动打标**（规则引擎，全部可配置）：
- 文号正则提取：`（20XX）XX号` / `XX令20XX年第X号` 等常见格式
- 发布机关：由文号前缀 + 正文抬头词表匹配
- 日期提取：成文日期/发布日期的关键词定位
- **分类自动映射**：预置"关键词 → 分类"映射表（如命中"反洗钱、洗钱、可疑交易"→ C05），多重命中取权重最高为主分类
- **效力状态初判**：正文含"本办法自××起施行，原××同时废止"→ 可自动解析出被废止对象，状态初判留人工确认
**④ 质量校验**：必填字段完整性检查 → 不合格记录标记原因，导出"待补清单"
**⑤ 入库 + 关联**：按《法规库数据库设计.sql》事务写入，同时维护 FTS 全文索引

### 2.3 复用边界说明

- **法规类数据**（新法规、客户行制度）：管道直接复用，差异只在接入适配器
- **处罚案例**：字段不同，需走 penalty_case 表的另一套映射，但"②清洗 ③规则提取 ④校验"的逻辑框架共用

---

## 三、新法规发布后怎么入库、怎么处理

### 3.1 触发来源（三选一，先人工/半自动，稳定后再自动化）

1. 日常监控：监管官网（人行、金监总局、外汇局、证监会）的发布页，每周例行检查一次
2. 客户/同事转发的新规（PDF/链接）
3. 将来可做定时爬虫 + 关键词提醒（地基阶段不做）

### 3.2 入库处理流程（新规 ≠ 简单加一条，是"变更处理"）

```
新规到达
  ↓
① 录入：手动上传 / 半自动解析（文号·机关·日期·正文）
  ↓
② 查重：标题+文号 判重 → 重复则终止（可能只是旧文补录）
  ↓
③ 关联检测（重点）：解析正文里"废止/修订/替代"条款，
   列出受影响的历史法规清单 → 提交核验人
  ↓
④ 人工核验（合规岗，不可省）：
   · 效力状态定论（新规=有效，被废止旧规=失效/修订失效）
   · 分类标签确认（自动映射结果抽查）
   · 受影响清单确认（③的结果对不对）
  ↓
⑤ 事务入库（一个原子操作，要么全成功要么全回滚）：
   · 新规 INSERT（status=有效）
   · 旧规 UPDATE：status 改为 失效/部分修订
   · reg_relation INSERT：新规↔旧规 关系（废止/修订/替代）
   · FTS 索引同步更新
  ↓
⑥ 影响提示（发给使用方，一期人工周报即可）：
   · 本周新规清单
   · 被废止/修订的旧规清单（提示业务部门核对现行制度依据）
   · 提示存在行内制度映射的政策（将来 policy_reg_map 关联到的）
```

### 3.3 为什么必须"人工核验"这一关

自动解析能处理 90% 的常规情况，但 **"废止关系判定"是合规库最容易出错、出错最致命的一环**——新规说"原办法同时废止"，原文不一定指名道姓，可能写"此前发布的相关规定，与本办法不一致的，以本办法为准"。这种模糊关系必须由懂业务的人确认。**规则定死：效力变更必须过核验人，系统永不自动改状态。**

### 3.4 建议的角色分工（先轻后重）

| 角色 | 职责 | 谁承担 |
|------|------|--------|
| 录入/清洗（可脚本为主） | ①~③ 段 | 原型阶段：你自己 |
| 核验人 | ④ 效力与分类定论 | 原型阶段：你自己 + 找懂合规的人复核；产品化后必须是客户行合规部 |
| 管理员 | 流程运转、周报 | 同上 |

### 3.5 输出物（地基交付时一起交）

1. `法规库数据库设计.sql`（已就绪）
2. 本方案评审通过后的《分类体系表》（含关键词映射规则）
3. 清洗入库管道脚本 + 运行说明
4. 《新规入库操作规程》一页纸（供以后交接或给客户看流程）

---

## 四、待你确认的问题

1. 一级分类 12 类的划分是否符合你们爬的数据实际情况？（等数据到手用真实标题验证分类命中率）
2. 是否同意"效力变更必须人工核验"这条红线？
3. 必填字段清单（1.4 节）是否 OK？
